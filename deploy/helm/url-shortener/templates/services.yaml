apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-shortener.fullname" . }}-shortener-api
  labels:
    app.kubernetes.io/component: shortener-api
spec:
  selector:
    app.kubernetes.io/component: shortener-api
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-shortener.fullname" . }}-redirect-service
  labels:
    app.kubernetes.io/component: redirect-service
spec:
  selector:
    app.kubernetes.io/component: redirect-service
  ports:
    - port: 80
      targetPort: 8081

